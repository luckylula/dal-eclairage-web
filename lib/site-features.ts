/**
 * Sections optionnelles du site.
 * Passer à `true` pour réafficher une section sans retoucher le menu ni les pages.
 */
export const siteFeatures = {
  /** Page /actualites + lien dans le header et le footer. */
  actualites: false,
} as const;
