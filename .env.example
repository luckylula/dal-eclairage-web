# Neon — Dashboard → Connection details → « Connection string » (Pooled)
# Format : postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
# Si le mot de passe contient @ # : / % etc., il faut l’encoder (ex. @ → %40)
DATABASE_URL=

RESEND_API_KEY=
RESEND_FROM_EMAIL=
CONTACT_EMAIL=
ADMIN_API_KEY=
